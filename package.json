{
  "name": "greenacres-crm",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "npx wrangler deploy",
    "push": "git push origin main && npx wrangler deploy"
  }
}